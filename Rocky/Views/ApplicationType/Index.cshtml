@model IEnumerable<Rocky.Models.ApplicationType>;

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">ApplicationType list</h2>
        </div>

        <div class="col-6 text-right">
            <a class="btn-primary btn" asp-controller="ApplicationType" asp-action="Create">
                <i class="fa-solid fa-plus"></i> &nbsp; Create new Application Type
            </a> 
        </div>        
    </div>
    <br />

    @if(Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>Application Type Name</th>                    
                </tr>
            </thead>
            <tbody>
                @foreach(var obj in Model)
                {
                    <tr>
                        <td width="50%">@obj.Name</td>
                        
                        <td>
                            <div role="group" class="w-75 btn-group">
                                <a class="btn-primary btn mx-2" asp-route-id="@obj.Id" asp-controller="ApplicationType" asp-action="Edit">
                                    <i class="fa-solid fa-pen-to-square"></i>
                                    <a class="btn-danger btn mx-2" asp-route-id="@obj.Id" asp-controller="ApplicationType" asp-action="Delete">
                                    <i class="fa-solid fa-trash"></i>
                                </a> 
                                </a>
                                
                            </div>
                        </td>  
                    </tr>                                     
                }
            </tbody>
            
        </table>
    }
    else
    {
        <p>No Application Type exists</p>            
    } 
</div>



